<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title id="seo-title">Product | 247 AVL Plug</title>
    <meta name="description" id="seo-desc" content="">
    <meta name="robots" content="index, follow">
    <link id="seo-canonical" rel="canonical" href="">
    <meta property="og:type" content="product">
    <meta property="og:title" id="og-title" content="">
    <meta property="og:description" id="og-desc" content="">
    <meta property="og:image" id="og-image" content="">
    <meta property="og:url" id="og-url" content="">
    <meta name="twitter:card" content="summary_large_image">

    <style>
        * { box-sizing: border-box; margin: 0; padding: 0; }
        body {
            font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif;
            background-color: #FF9500;
            color: #1d1d1f;
            line-height: 1.6;
            min-height: 100vh;
            -webkit-font-smoothing: antialiased;
        }
        header {
            background-color: rgba(255,255,255,0.97);
            padding: 14px 20px;
            display: flex;
            align-items: center;
            justify-content: space-between;
            border-bottom: 1px solid #e5e5e7;
            position: sticky;
            top: 0;
            z-index: 100;
            box-shadow: 0 2px 12px rgba(0,0,0,0.08);
        }
        .back-btn {
            background-color: #fff;
            border: 1.5px solid #d1d1d6;
            color: #1d1d1f;
            padding: 10px 20px;
            border-radius: 50px;
            text-decoration: none;
            font-weight: 600;
            font-size: 14px;
            display: inline-flex;
            align-items: center;
            gap: 8px;
        }
        .header-title {
            font-size: 15px;
            font-weight: 600;
            color: #6e6e73;
            white-space: nowrap;
            overflow: hidden;
            text-overflow: ellipsis;
            max-width: 50%;
        }
        main {
            max-width: 1200px;
            margin: 0 auto;
            padding: 28px 16px 0;
        }
        article {
            display: flex;
            flex-direction: column;
            gap: 24px;
        }
        .card {
            background-color: #ffffff;
            border-radius: 24px;
            padding: 28px;
            box-shadow: 0 8px 32px rgba(0,0,0,0.12);
        }
        .product-image-container {
            padding: 20px;
            background-color: #f5f5f7;
            display: flex;
            align-items: center;
            justify-content: center;
        }
        .image-wrapper {
            position: relative;
            width: 100%;
            max-width: 500px;
            border-radius: 16px;
            overflow: hidden;
            box-shadow: 0 8px 24px rgba(0,0,0,0.15);
            background-color: #e5e5e7;
        }
        .product-image {
            width: 100%;
            height: auto;
            display: block;
            object-fit: cover;
            aspect-ratio: 1 / 1;
        }
        .image-badge {
            position: absolute;
            top: 14px;
            left: 14px;
            background-color: #25D366;
            color: #fff;
            font-size: 12px;
            font-weight: 800;
            padding: 7px 16px;
            border-radius: 50px;
            text-transform: uppercase;
            letter-spacing: 0.8px;
            box-shadow: 0 4px 12px rgba(37,211,102,0.4);
            z-index: 2;
        }
        .product-details {
            padding: 32px 28px;
        }
        .product-title {
            font-size: 34px;
            font-weight: 800;
            margin-bottom: 6px;
            line-height: 1.15;
            color: #1d1d1f;
        }
        .product-subtitle {
            font-size: 15px;
            color: #6e6e73;
            margin-bottom: 24px;
            font-weight: 500;
        }
        .product-description {
            color: #6e6e73;
            font-size: 16px;
            line-height: 1.7;
            margin-bottom: 28px;
            padding: 22px;
            background-color: #f5f5f7;
            border-radius: 16px;
            border-left: 4px solid #25D366;
        }
        .section-label {
            font-size: 11px;
            font-weight: 700;
            text-transform: uppercase;
            letter-spacing: 1.5px;
            color: #86868b;
            margin-bottom: 14px;
        }
        .buy-buttons {
            display: flex;
            gap: 12px;
            margin-top: 4px;
        }
        .contact-btn {
            flex: 1;
            padding: 18px 20px;
            border-radius: 14px;
            text-align: center;
            text-decoration: none;
            font-weight: 700;
            font-size: 16px;
            display: inline-flex;
            align-items: center;
            justify-content: center;
            gap: 10px;
            border: none;
            cursor: pointer;
        }
        .btn-whatsapp {
            background-color: #25D366;
            color: #fff;
        }
        .btn-telegram {
            background-color: #0088cc;
            color: #fff;
        }

        /* ============================================================ */
        /*  FAQ ACCORDION                                               */
        /* ============================================================ */
        .faq-section {
            margin-bottom: 28px;
        }
        .faq-item {
            background-color: #ffffff;
            border: 1.5px solid #e5e5e7;
            border-radius: 12px;
            margin-bottom: 10px;
            overflow: hidden;
            transition: all 0.2s ease;
        }
        .faq-item:hover {
            border-color: #d1d1d6;
        }
        .faq-question {
            width: 100%;
            padding: 16px 20px;
            background: none;
            border: none;
            text-align: left;
            font-size: 15px;
            font-weight: 700;
            color: #1d1d1f;
            cursor: pointer;
            display: flex;
            justify-content: space-between;
            align-items: center;
            gap: 12px;
        }
        .faq-icon {
            font-size: 18px;
            font-weight: 800;
            color: #86868b;
            flex-shrink: 0;
            transition: transform 0.25s ease;
        }
        .faq-item.open .faq-icon {
            transform: rotate(45deg);
            color: #25D366;
        }
        .faq-answer {
            max-height: 0;
            overflow: hidden;
            transition: max-height 0.3s ease, padding 0.3s ease;
        }
        .faq-item.open .faq-answer {
            max-height: 300px;
        }
        .faq-answer-inner {
            padding: 0 20px 16px;
            color: #6e6e73;
            font-size: 14px;
            line-height: 1.7;
        }

        /* ============================================================ */
        /*  FEATURED PRODUCTS GRID                                      */
        /* ============================================================ */
        .featured-products-grid {
            display: flex;
            flex-wrap: wrap;
            gap: 12px;
            margin-top: 16px;
        }
        .featured-product-card {
            flex: 1 1 calc(50% - 6px);
            min-width: 130px;
            max-width: calc(50% - 6px);
            background-color: #ffffff;
            border: 2px solid #e5e5e7;
            border-radius: 16px;
            overflow: hidden;
            text-decoration: none;
            box-shadow: 0 3px 10px rgba(0,0,0,0.08);
            transition: all 0.25s ease;
        }
        .featured-product-card:hover {
            transform: translateY(-4px);
            box-shadow: 0 10px 28px rgba(0,0,0,0.14);
            border-color: #25D366;
        }
        .featured-product-card .fp-img-wrap {
            padding: 10px;
            background: linear-gradient(145deg, #fafafa 0%, #f0f0f2 100%);
        }
        .featured-product-card img {
            width: 100%;
            height: auto;
            aspect-ratio: 1 / 1;
            object-fit: cover;
            display: block;
            border-radius: 10px;
            border: 1px solid #e5e5e7;
            background-color: #e5e5e7;
        }
        .featured-product-card h3 {
            font-size: 13px;
            font-weight: 700;
            color: #1d1d1f;
            padding: 12px;
            text-align: center;
            line-height: 1.3;
        }

        /* ============================================================ */
        /*  DISCLAIMER BAR — BOTTOM OF PAGE                             */
        /* ============================================================ */
        .disclaimer-bar {
            max-width: 1200px;
            margin: 0 auto;
            padding: 40px 20px 60px;
            text-align: center;
        }
        .disclaimer-bar p {
            font-size: 12px;
            color: rgba(255,255,255,0.85);
            line-height: 1.7;
            max-width: 800px;
            margin: 0 auto;
        }
        .disclaimer-bar strong {
            color: #ffffff;
            font-weight: 700;
            display: block;
            margin-bottom: 8px;
            font-size: 13px;
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }

        @media (min-width: 768px) {
            main { padding: 40px 28px 0; }
            article { display: grid; grid-template-columns: 1fr 1fr; gap: 32px; align-items: start; }
            .product-image-container { position: sticky; top: 80px; }
            .product-details { padding: 40px 36px; }
            .product-title { font-size: 40px; }
            .featured-product-card {
                flex: 1 1 calc(25% - 9px);
                min-width: 90px;
                max-width: calc(25% - 9px);
            }
            .featured-product-card h3 {
                font-size: 14px;
                padding: 14px;
            }
        }
        @media (min-width: 1200px) {
            main { max-width: 1280px; padding: 48px 40px 0; }
            article { gap: 40px; }
            .product-title { font-size: 44px; }
        }
        @media (max-width: 480px) {
            .product-title { font-size: 26px; }
            .product-details { padding: 24px 20px; }
            .card { padding: 20px; border-radius: 20px; }
            .buy-buttons { flex-direction: column; }
            .contact-btn { width: 100%; }
            .featured-product-card h3 {
                font-size: 12px;
                padding: 10px;
            }
            .faq-question { font-size: 14px; padding: 14px 16px; }
            .faq-answer-inner { padding: 0 16px 14px; }
        }
    </style>
<base target="_blank">
</head>
<body>

    <header>
        <a href="/" class="back-btn">&#8592; Back to Shop</a>
        <h1 id="header-title" class="header-title"></h1>
    </header>

    <main>
        <article>
            <div class="card product-image-container">
                <div class="image-wrapper">
                    <span class="image-badge" id="img-badge">In Stock</span>
                    <img id="p-img" src="" alt="" class="product-image">
                </div>
            </div>

            <section class="card product-details">
                <h2 id="p-name" class="product-title"></h2>
                <p id="p-subtitle" class="product-subtitle"></p>

                <div class="section-label">Key Features</div>
                <div id="feature-bubbles"></div>

                <p id="p-desc" class="product-description"></p>

                <!-- FAQ ACCORDION -->
                <div class="section-label">Frequently Asked Questions</div>
                <div id="faq-container" class="faq-section"></div>

                <div class="section-label">Choose Your Package</div>
                <div id="pricing-tiers"></div>

                <div class="buy-buttons">
                    <a id="btn-wa" href="#" class="contact-btn btn-whatsapp" target="_blank" rel="noopener">
                        <span style="font-size:20px;">&#128172;</span> WhatsApp
                    </a>
                    <a id="btn-tg" href="#" class="contact-btn btn-telegram" target="_blank" rel="noopener">
                        <span style="font-size:20px;">&#9992;</span> Telegram
                    </a>
                </div>

                <!-- FEATURED PRODUCTS -->
                <div class="section-label" style="margin-top:32px;">You May Also Like</div>
                <div id="featured-products" class="featured-products-grid"></div>

                <!-- FOOTER POLICY LINKS -->
                <div id="footer-links"></div>
            </section>
        </article>
    </main>

    <!-- DISCLAIMER — sits on the orange background at the very bottom -->
    <div class="disclaimer-bar">
        <p id="disclaimer-text"></p>
    </div>

    <!-- ================================================================ -->
    -->
    <!--   EDIT ONLY THIS BLOCK FOR NEW PRODUCTS                          -->
    -->
    <!--   Each section is labeled. Change the values inside quotes.      -->
    -->
    <!-- ================================================================ -->
    <script>
        const PRODUCT_CONFIG = {

            /* ========================================================= */
            /*  1. BASIC INFO                                            */
            /* ========================================================= */
            name: "97% Pure Colombian Cocaine For Sale | Best Price/ 1KG",
            subtitle: "uncut cocaine | Maximun purity | Discreet delivery",

            /* ========================================================= */
            /*  2. IMAGES                                                */
            /* ========================================================= */
            imageSrc: "https://github.com/respectedgenre1/247avlplug/blob/main/public/Colombian%20Cocaine%20for%20Sale.jpeg?raw=true",
            imageAlt: "Uncut Colombian Cocaine for Sale | 247 AVL Plug",
            imageBadge: "In Stock",

            /* ========================================================= */
            /*  3. SEO & META                                            */
            /* ========================================================= */
            seoTitle: "Buy up to 97% Pure Colombian Cocaine Online | 247 AVL Plug",
            seoDescription: "Pure Colombian Cocaine for sale with express discreet delivery to any address (Home, Hotels, AirBnB, Dead drops).Best prices /kg and Discount deals on 247 AVL PLUG",
            urlSlug: "buy-colombian-cocaine-online.html",

            /* ========================================================= */
            /*  4. PRICING TIERS                                         */
            /* ========================================================= */
            pricingTiers: [
                { label: "1 Gram",    price: "$105.00",              badge: "" },
                { label: "5 Grams",   price: "$380.00",            badge: "good deal" },
                { label: "10 Grams",  price: "$690.00",            badge: "Best deal" },
                { label: "25 Grams",  price: "$1749.99",            badge: "Free 1g" },
                { label: "50+ Grams", price: "Contact for Quote",  badge: "Wholesale" }
            ],
            defaultTierIndex: 0,

            /* ========================================================= */
            /*  5. DESCRIPTION                                           */
            /* ========================================================= */
            description: "247 AVL PLUG specializes in Discreet delivery of Colombian cocaine, Peruvian Cocaine, Bolivian Cocaine and Pure Uncut Mexican Coke worldwide. We prioritize providing a secure online space, ensuring customer privacy and ease of access throughout each transaction. We Deliver Uncut Colombian coke to all cities with Same day to 5 days delivery, depending on city",

            /* ========================================================= */
            /*  6. FAQS — Add as many Q&A pairs as needed                */
            /* ========================================================= */
            faqs: [
                {
                    question: "How Pure is your Colombian Cocaine?",
                    answer: "We ensure up to 98% purity for our uncut Colombian cocaine, sourced directly from Colombia and smuggled to Western markets through discreet routes. This guarantees unmatched quality and purity. Our discreet global delivery prioritizes your confidentiality and product excellence.."
                },
                {
                    question: "How Discreet is packging and What is delivery time",
                    answer: "strong|High Purity | Uncut Cocaine |Packaging: Vacuum-sealed, completely discreet | Discreet Shipping: Worldwide with full tracking provided. We offer various shipping options including standard, express, and overnight shipping within the USA,Canada, and UK. With 3-5 days Express delivery to other country in Europe, Australia and the Middle east."
                },
                {
                    question: "What payment methods do you accept?",
                    answer: "We accept Bitcoin, Ethereum, and other major cryptocurrencies for maximum privacy. Contact us via WhatsApp or Telegram for payment instructions after selecting your package."
                },
                {
                    question: "Is this product legal in my country?",
                    answer: "It is your responsibility to verify the legal status of research chemicals in your jurisdiction before ordering. We sell strictly for research purposes and comply with all applicable laws."
                }
            ],

            /* ========================================================= */
            /*  7. FEATURES (3 items max)                                */
            /* ========================================================= */
            features: [
                "High Purity",
                "Discreet Packaging",
                "Any Address Delivery"
            ],
            featureEmojis: ["&#128142;", "&#128230;", "&#128666;"],

            /* ========================================================= */
            /*  8. CONTACT — EDIT THESE FOR YOUR ACCOUNTS                */
            /* ========================================================= */
            whatsappNumber: "13254365583",
            telegramHandle: "datplugg",

            /* ========================================================= */
            /*  9. FEATURED PRODUCTS                                     */
            /*     4 products shown below contact buttons                */
            /* ========================================================= */
            featuredProducts: [
                { name: "Cocaine For Sale", image: "https://github.com/respectedgenre1/247avlplug/blob/main/public/Buy%20colombian%20cocaine.jpeg?raw=true"", url: "/buy-cocaine-online.html" },
                { name: "Buy Ketamine Online", image: "https://github.com/respectedgenre1/247avlplug/blob/main/public/Ketamine%20for%20sale.jpeg?raw=true", url: "/buy-ketamine.html" },
                { name: "Buy Research Chemicals", image: "https://github.com/respectedgenre1/247avlplug/blob/main/public/Research%20Chemicals%20For%20sale.jpeg?raw=true", url: "/buy-research-chemicals.html" },
                { name: "Buy THC Distillates", image: "https://github.com/respectedgenre1/247avlplug/blob/main/public/THC%20Distillates%20For%20sale.jpeg?raw=true", url: "/buy-thc-distillates.html" },
            ],

            /* ========================================================= */
            /*  10. FOOTER POLICY LINKS                                  */
            /* ========================================================= */
            footerLinks: [
                { text: "Privacy Policy",                 url: "/privacy-policy.html" },
                { text: "Payment and Refund Policy",      url: "/payment-refund-policy.html" },
                { text: "Terms and Conditions",           url: "/terms-and-conditions.html" },
                { text: "Shipping and Delivery Policies", url: "/shipping-delivery-policy.html" }
            ],

            /* ========================================================= */
            /*  11. DISCLAIMER — Edit this text as needed                */
            /* ========================================================= */
            disclaimer: "Cocaine is illegal is every country worldwide. So is trying to buy cocaine or sell cocaine online. We will take full responsibilities for our action and we hope all who is accessing this page to solicite for Cocaine delivery online is fully aware of the consiquences of being caught with this products.",

            /* ========================================================= */
            /*  12. THEME COLORS                                         */
            /* ========================================================= */
            themeColor: "#25D366",
            backgroundColor: "#FF9500"
        };

        /* ================================================================ */
        /*  ENGINE — DO NOT EDIT BELOW THIS LINE                            */
        /* ================================================================ */
        document.addEventListener('DOMContentLoaded', function() {
            var p = PRODUCT_CONFIG;
            var baseUrl = "https://247avlplug.com";
            var productUrl = baseUrl + "/products/" + p.urlSlug;
            var imageUrl = p.imageSrc.startsWith('http') ? p.imageSrc : baseUrl + p.imageSrc;

            document.body.style.backgroundColor = p.backgroundColor;

            // SEO
            document.title = p.seoTitle;
            document.getElementById('seo-desc').content = p.seoDescription;
            document.getElementById('seo-canonical').href = productUrl;
            document.getElementById('og-title').content = p.seoTitle;
            document.getElementById('og-desc').content = p.seoDescription;
            document.getElementById('og-image').content = imageUrl;
            document.getElementById('og-url').content = productUrl;

            // Content
            document.getElementById('header-title').textContent = p.name;
            document.getElementById('p-name').textContent = p.name;
            document.getElementById('p-subtitle').textContent = p.subtitle;
            document.getElementById('p-desc').textContent = p.description;

            var imgEl = document.getElementById('p-img');
            imgEl.src = imageUrl;
            imgEl.alt = p.imageAlt;

            var badgeEl = document.getElementById('img-badge');
            if (p.imageBadge && p.imageBadge.trim() !== "") {
                badgeEl.textContent = p.imageBadge;
                badgeEl.style.display = "inline-block";
            } else {
                badgeEl.style.display = "none";
            }

            // Disclaimer
            var disclaimerEl = document.getElementById('disclaimer-text');
            disclaimerEl.innerHTML = '<strong>Disclaimer</strong>' + p.disclaimer;

            /* ========================================================= */
            /*  FEATURE CARDS                                            */
            /* ========================================================= */
            var featuresContainer = document.getElementById('feature-bubbles');
            featuresContainer.innerHTML = '';
            featuresContainer.style.display = 'flex';
            featuresContainer.style.flexWrap = 'wrap';
            featuresContainer.style.gap = '12px';
            featuresContainer.style.marginBottom = '28px';

            var emojis = p.featureEmojis || ["&#10003;", "&#10003;", "&#10003;"];

            if (p.features && p.features.length > 0) {
                for (var i = 0; i < p.features.length; i++) {
                    var card = document.createElement('div');
                    card.style.backgroundColor = '#ffffff';
                    card.style.border = '1.5px solid #e5e5e7';
                    card.style.borderTop = '4px solid #25D366';
                    card.style.borderRadius = '14px';
                    card.style.padding = '18px 16px';
                    card.style.flex = '1 1 140px';
                    card.style.minWidth = '140px';
                    card.style.textAlign = 'center';
                    card.style.boxShadow = '0 2px 8px rgba(0,0,0,0.06)';
                    card.style.transition = 'all 0.25s ease';
                    card.style.cursor = 'default';

                    card.innerHTML =
                        '<div style="font-size:28px;margin-bottom:8px;">' + emojis[i] + '</div>' +
                        '<div style="font-size:13px;font-weight:700;color:#1d1d1f;line-height:1.4;">' + p.features[i] + '</div>';

                    featuresContainer.appendChild(card);
                }
            }

            /* ========================================================= */
            /*  FAQ ACCORDION                                            */
            /* ========================================================= */
            var faqContainer = document.getElementById('faq-container');
            faqContainer.innerHTML = '';

            if (p.faqs && p.faqs.length > 0) {
                for (var q = 0; q < p.faqs.length; q++) {
                    var faqItem = document.createElement('div');
                    faqItem.className = 'faq-item';
                    faqItem.style.backgroundColor = '#ffffff';
                    faqItem.style.border = '1.5px solid #e5e5e7';
                    faqItem.style.borderRadius = '12px';
                    faqItem.style.marginBottom = '10px';
                    faqItem.style.overflow = 'hidden';
                    faqItem.style.transition = 'all 0.2s ease';

                    var btn = document.createElement('button');
                    btn.type = 'button';
                    btn.className = 'faq-question';
                    btn.style.width = '100%';
                    btn.style.padding = '16px 20px';
                    btn.style.background = 'none';
                    btn.style.border = 'none';
                    btn.style.textAlign = 'left';
                    btn.style.fontSize = '15px';
                    btn.style.fontWeight = '700';
                    btn.style.color = '#1d1d1f';
                    btn.style.cursor = 'pointer';
                    btn.style.display = 'flex';
                    btn.style.justifyContent = 'space-between';
                    btn.style.alignItems = 'center';
                    btn.style.gap = '12px';
                    btn.innerHTML = '<span>' + p.faqs[q].question + '</span><span class="faq-icon" style="font-size:18px;font-weight:800;color:#86868b;flex-shrink:0;transition:transform 0.25s ease;">+</span>';

                    var answerWrap = document.createElement('div');
                    answerWrap.className = 'faq-answer';
                    answerWrap.style.maxHeight = '0';
                    answerWrap.style.overflow = 'hidden';
                    answerWrap.style.transition = 'max-height 0.3s ease, padding 0.3s ease';

                    var answerInner = document.createElement('div');
                    answerInner.className = 'faq-answer-inner';
                    answerInner.style.padding = '0 20px 16px';
                    answerInner.style.color = '#6e6e73';
                    answerInner.style.fontSize = '14px';
                    answerInner.style.lineHeight = '1.7';
                    answerInner.textContent = p.faqs[q].answer;

                    answerWrap.appendChild(answerInner);
                    faqItem.appendChild(btn);
                    faqItem.appendChild(answerWrap);

                    btn.addEventListener('click', function(item, ans, icon) {
                        return function() {
                            var isOpen = item.classList.contains('open');
                            // Close all
                            var allFaqs = faqContainer.querySelectorAll('.faq-item');
                            for (var a = 0; a < allFaqs.length; a++) {
                                allFaqs[a].classList.remove('open');
                                allFaqs[a].style.borderColor = '#e5e5e7';
                                var aAns = allFaqs[a].querySelector('.faq-answer');
                                if (aAns) aAns.style.maxHeight = '0';
                                var aIcon = allFaqs[a].querySelector('.faq-icon');
                                if (aIcon) {
                                    aIcon.style.transform = 'rotate(0deg)';
                                    aIcon.style.color = '#86868b';
                                }
                            }
                            // Toggle current
                            if (!isOpen) {
                                item.classList.add('open');
                                item.style.borderColor = '#25D366';
                                ans.style.maxHeight = '300px';
                                icon.style.transform = 'rotate(45deg)';
                                icon.style.color = '#25D366';
                            }
                        };
                    }(faqItem, answerWrap, btn.querySelector('.faq-icon')));

                    faqContainer.appendChild(faqItem);
                }
            }

            /* ========================================================= */
            /*  PRICING TIERS                                            */
            /* ========================================================= */
            var tiersContainer = document.getElementById('pricing-tiers');
            tiersContainer.innerHTML = '';
            tiersContainer.style.display = 'flex';
            tiersContainer.style.flexDirection = 'column';
            tiersContainer.style.gap = '10px';
            tiersContainer.style.marginBottom = '28px';

            var currentTier = p.pricingTiers[p.defaultTierIndex];

            for (var i = 0; i < p.pricingTiers.length; i++) {
                (function(tier, index) {
                    var row = document.createElement('div');
                    var isSelected = (index === p.defaultTierIndex);

                    row.style.backgroundColor = isSelected ? '#e8f8ee' : '#ffffff';
                    row.style.border = isSelected ? '2px solid #25D366' : '2px solid #d1d1d6';
                    row.style.borderLeft = isSelected ? '5px solid #25D366' : '2px solid #d1d1d6';
                    row.style.borderRadius = '12px';
                    row.style.padding = '16px 18px';
                    row.style.cursor = 'pointer';
                    row.style.display = 'flex';
                    row.style.alignItems = 'center';
                    row.style.justifyContent = 'space-between';
                    row.style.gap = '12px';
                    row.style.boxShadow = isSelected ? '0 4px 12px rgba(37,211,102,0.15)' : '0 1px 4px rgba(0,0,0,0.04)';
                    row.style.transition = 'all 0.2s ease';

                    var radioColor = isSelected ? '#25D366' : '#d1d1d6';
                    var radioInner = isSelected ? '<div style="width:10px;height:10px;background:#fff;border-radius:50%;"></div>' : '';

                    var badgeHtml = '';
                    if (tier.badge && tier.badge.trim() !== '') {
                        badgeHtml = '<span style="font-size:11px;background:#25D366;color:#fff;padding:3px 10px;border-radius:50px;font-weight:800;text-transform:uppercase;letter-spacing:0.5px;white-space:nowrap;">' + tier.badge + '</span>';
                    }

                    row.innerHTML =
                        '<div style="display:flex;align-items:center;gap:12px;flex:1;">' +
                            '<div style="width:22px;height:22px;border-radius:50%;border:3px solid ' + radioColor + ';background:' + radioColor + ';display:flex;align-items:center;justify-content:center;flex-shrink:0;">' + radioInner + '</div>' +
                            '<div style="display:flex;align-items:center;gap:8px;flex-wrap:wrap;">' +
                                '<span style="font-weight:700;font-size:15px;color:#1d1d1f;white-space:nowrap;">' + tier.label + '</span>' +
                                badgeHtml +
                            '</div>' +
                        '</div>' +
                        '<span style="font-weight:800;font-size:17px;color:#1faa52;white-space:nowrap;flex-shrink:0;">' + tier.price + '</span>';

                    row.addEventListener('click', function() {
                        var allRows = tiersContainer.children;
                        for (var j = 0; j < allRows.length; j++) {
                            var r = allRows[j];
                            r.style.backgroundColor = '#ffffff';
                            r.style.border = '2px solid #d1d1d6';
                            r.style.borderLeft = '2px solid #d1d1d6';
                            r.style.boxShadow = '0 1px 4px rgba(0,0,0,0.04)';
                            var radio = r.querySelector('div > div');
                            if (radio) {
                                radio.style.borderColor = '#d1d1d6';
                                radio.style.background = '#d1d1d6';
                                radio.innerHTML = '';
                            }
                        }

                        row.style.backgroundColor = '#e8f8ee';
                        row.style.border = '2px solid #25D366';
                        row.style.borderLeft = '5px solid #25D366';
                        row.style.boxShadow = '0 4px 12px rgba(37,211,102,0.15)';
                        var activeRadio = row.querySelector('div > div');
                        if (activeRadio) {
                            activeRadio.style.borderColor = '#25D366';
                            activeRadio.style.background = '#25D366';
                            activeRadio.innerHTML = '<div style="width:10px;height:10px;background:#fff;border-radius:50%;"></div>';
                        }

                        updateLinks(tier);
                    });

                    tiersContainer.appendChild(row);
                })(p.pricingTiers[i], i);
            }

            /* ========================================================= */
            /*  FEATURED PRODUCTS                                        */
            /* ========================================================= */
            var featuredContainer = document.getElementById('featured-products');
            featuredContainer.innerHTML = '';

            if (p.featuredProducts && p.featuredProducts.length > 0) {
                for (var f = 0; f < p.featuredProducts.length; f++) {
                    var fp = p.featuredProducts[f];
                    var fpImage = fp.image.startsWith('http') ? fp.image : baseUrl + fp.image;

                    var link = document.createElement('a');
                    link.href = fp.url;
                    link.className = 'featured-product-card';

                    link.innerHTML =
                        '<div class="fp-img-wrap" style="padding:10px;background:linear-gradient(145deg,#fafafa 0%,#f0f0f2 100%);">' +
                            '<img src="' + fpImage + '" alt="' + fp.name + '" loading="lazy" style="width:100%;height:auto;aspect-ratio:1/1;object-fit:cover;display:block;border-radius:10px;border:1px solid #e5e5e7;background-color:#e5e5e7;">' +
                        '</div>' +
                        '<h3 style="font-size:13px;font-weight:700;color:#1d1d1f;padding:12px;text-align:center;line-height:1.3;">' + fp.name + '</h3>';

                    featuredContainer.appendChild(link);
                }
            }

            /* ========================================================= */
            /*  FOOTER POLICY LINKS                                      */
            /* ========================================================= */
            var footerContainer = document.getElementById('footer-links');
            footerContainer.innerHTML = '';
            footerContainer.style.marginTop = '28px';
            footerContainer.style.paddingTop = '22px';
            footerContainer.style.borderTop = '1px solid #e5e5e7';
            footerContainer.style.display = 'flex';
            footerContainer.style.flexWrap = 'wrap';
            footerContainer.style.justifyContent = 'center';
            footerContainer.style.gap = '8px 20px';

            if (p.footerLinks && p.footerLinks.length > 0) {
                for (var k = 0; k < p.footerLinks.length; k++) {
                    var link = document.createElement('a');
                    link.href = p.footerLinks[k].url;
                    link.textContent = p.footerLinks[k].text;
                    link.style.color = '#86868b';
                    link.style.fontSize = '12px';
                    link.style.fontWeight = '600';
                    link.style.textDecoration = 'none';
                    link.style.transition = 'color 0.2s ease';
                    link.style.whiteSpace = 'nowrap';

                    link.addEventListener('mouseenter', function() {
                        this.style.color = '#25D366';
                    });
                    link.addEventListener('mouseleave', function() {
                        this.style.color = '#86868b';
                    });

                    footerContainer.appendChild(link);
                }
            }

            /* ========================================================= */
            /*  CONTACT LINKS                                            */
            /* ========================================================= */
            function updateLinks(selectedTier) {
                var waMessage = encodeURIComponent(
                    'Hi! I want to buy the ' + p.name + ' (' + selectedTier.label + ' - ' + selectedTier.price + ') from 247avlplug.com'
                );
                document.getElementById('btn-wa').href = 'https://wa.me/' + p.whatsappNumber + '?text=' + waMessage;

                var tgMessage = encodeURIComponent(
                    'Hi! I want to buy the ' + p.name + ' (' + selectedTier.label + ' - ' + selectedTier.price + ') from 247avlplug.com'
                );
                document.getElementById('btn-tg').href = 'https://t.me/' + p.telegramHandle + '?text=' + tgMessage;
            }

            updateLinks(currentTier);
        });
    </script>
</body>
</html>
