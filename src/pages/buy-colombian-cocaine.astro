<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title id="seo-title">Product | 247 AVL Plug</title>
    <meta name="description" id="seo-desc" content="">
    <meta name="robots" content="index, follow">
    <link id="seo-canonical" rel="canonical" href="">
    <meta property="og:type" content="product">
    <meta property="og:title" id="og-title" content="">
    <meta property="og:description" id="og-desc" content="">
    <meta property="og:image" id="og-image" content="">
    <meta property="og:url" id="og-url" content="">
    <meta name="twitter:card" content="summary_large_image">

    <style>
        * { box-sizing: border-box; margin: 0; padding: 0; }
        body {
            font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif;
            background-color: #FF9500;
            color: #1d1d1f;
            line-height: 1.6;
            min-height: 100vh;
            -webkit-font-smoothing: antialiased;
        }
        header {
            background-color: rgba(255,255,255,0.97);
            padding: 14px 20px;
            display: flex;
            align-items: center;
            justify-content: space-between;
            border-bottom: 1px solid #e5e5e7;
            position: sticky;
            top: 0;
            z-index: 100;
            box-shadow: 0 2px 12px rgba(0,0,0,0.08);
        }
        .back-btn {
            background-color: #fff;
            border: 1.5px solid #d1d1d6;
            color: #1d1d1f;
            padding: 10px 20px;
            border-radius: 50px;
            text-decoration: none;
            font-weight: 600;
            font-size: 14px;
            display: inline-flex;
            align-items: center;
            gap: 8px;
        }
        .header-title {
            font-size: 15px;
            font-weight: 600;
            color: #6e6e73;
            white-space: nowrap;
            overflow: hidden;
            text-overflow: ellipsis;
            max-width: 50%;
        }
        main {
            max-width: 1200px;
            margin: 0 auto;
            padding: 28px 16px 0;
        }
        article {
            display: flex;
            flex-direction: column;
            gap: 24px;
        }
        .card {
            background-color: #ffffff;
            border-radius: 24px;
            padding: 28px;
            box-shadow: 0 8px 32px rgba(0,0,0,0.12);
        }
        .product-image-container {
            padding: 20px;
            background-color: #f5f5f7;
            display: flex;
            align-items: center;
            justify-content: center;
        }
        .image-wrapper {
            position: relative;
            width: 100%;
            max-width: 500px;
            border-radius: 16px;
            overflow: hidden;
            box-shadow: 0 8px 24px rgba(0,0,0,0.15);
            background-color: #e5e5e7;
        }
        .product-image {
            width: 100%;
            height: auto;
            display: block;
            object-fit: cover;
            aspect-ratio: 1 / 1;
        }
        .image-badge {
            position: absolute;
            top: 14px;
            left: 14px;
            background-color: #25D366;
            color: #fff;
            font-size: 12px;
            font-weight: 800;
            padding: 7px 16px;
            border-radius: 50px;
            text-transform: uppercase;
            letter-spacing: 0.8px;
            box-shadow: 0 4px 12px rgba(37,211,102,0.4);
            z-index: 2;
        }
        .product-details {
            padding: 32px 28px;
        }
        .product-title {
            font-size: 34px;
            font-weight: 800;
            margin-bottom: 6px;
            line-height: 1.15;
            color: #1d1d1f;
        }
        .product-subtitle {
            font-size: 15px;
            color: #6e6e73;
            margin-bottom: 24px;
            font-weight: 500;
        }
        .product-description {
            color: #6e6e73;
            font-size: 16px;
            line-height: 1.7;
            margin-bottom: 28px;
            padding: 22px;
            background-color: #f5f5f7;
            border-radius: 16px;
            border-left: 4px solid #25D366;
        }
        .section-label {
            font-size: 11px;
            font-weight: 700;
            text-transform: uppercase;
            letter-spacing: 1.5px;
            color: #86868b;
            margin-bottom: 14px;
        }
        .buy-buttons {
            display: flex;
            gap: 12px;
            margin-top: 4px;
        }
        .contact-btn {
            flex: 1;
            padding: 18px 20px;
            border-radius: 14px;
            text-align: center;
            text-decoration: none;
            font-weight: 700;
            font-size: 16px;
            display: inline-flex;
            align-items: center;
            justify-content: center;
            gap: 10px;
            border: none;
            cursor: pointer;
        }
        .btn-whatsapp {
            background-color: #25D366;
            color: #fff;
        }
        .btn-telegram {
            background-color: #0088cc;
            color: #fff;
        }

        /* ============================================================ */
        /*  PRODUCT DETAILS & FAQ — ORGANIZED CARDS                     */
        /* ============================================================ */
        .faq-section {
            margin-bottom: 28px;
        }
        .faq-card {
            background-color: #ffffff;
            border: 1.5px solid #e5e5e7;
            border-left: 4px solid #25D366;
            border-radius: 12px;
            padding: 20px;
            margin-bottom: 12px;
        }
        .faq-question {
            font-size: 16px;
            font-weight: 800;
            color: #1d1d1f;
            margin-bottom: 10px;
            line-height: 1.4;
        }
        .faq-answer {
            font-size: 14px;
            color: #6e6e73;
            line-height: 1.7;
            padding-top: 10px;
            border-top: 1px solid #f0f0f2;
        }

        /* Featured Products */
        .featured-products-grid {
            display: grid;
            grid-template-columns: repeat(2, 1fr);
            gap: 14px;
            margin-top: 16px;
        }
        .featured-product-btn {
            display: block;
            background-color: #ffffff;
            border: 2px solid #FFD700;
            border-radius: 12px;
            padding: 16px 14px;
            text-align: center;
            text-decoration: none;
            color: #1d1d1f;
            font-size: 13px;
            font-weight: 700;
            box-shadow: 0 4px 12px rgba(255, 215, 0, 0.2);
            transition: all 0.25s ease;
            line-height: 1.3;
        }
        .featured-product-btn:hover {
            background-color: #fff9c4;
            border-color: #FFC107;
            transform: translateY(-3px);
            box-shadow: 0 8px 20px rgba(255, 215, 0, 0.35);
            color: #1d1d1f;
        }

        /* Disclaimer */
        .disclaimer-bar {
            max-width: 1200px;
            margin: 0 auto;
            padding: 40px 20px 60px;
            text-align: center;
        }
        .disclaimer-bar p {
            font-size: 12px;
            color: rgba(255,255,255,0.85);
            line-height: 1.7;
            max-width: 800px;
            margin: 0 auto;
        }
        .disclaimer-bar strong {
            color: #ffffff;
            font-weight: 700;
            display: block;
            margin-bottom: 8px;
            font-size: 13px;
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }

        @media (min-width: 768px) {
            main { padding: 40px 28px 0; }
            article { display: grid; grid-template-columns: 1fr 1fr; gap: 32px; align-items: start; }
            .product-image-container { position: sticky; top: 80px; }
            .product-details { padding: 40px 36px; }
            .product-title { font-size: 40px; }
            .featured-products-grid { grid-template-columns: repeat(4, 1fr); gap: 14px; }
            .featured-product-btn { font-size: 14px; padding: 18px 16px; }
            .faq-question { font-size: 17px; }
            .faq-answer { font-size: 15px; }
        }
        @media (min-width: 1200px) {
            main { max-width: 1280px; padding: 48px 40px 0; }
            article { gap: 40px; }
            .product-title { font-size: 44px; }
        }
        @media (max-width: 480px) {
            .product-title { font-size: 26px; }
            .product-details { padding: 24px 20px; }
            .card { padding: 20px; border-radius: 20px; }
            .buy-buttons { flex-direction: column; }
            .contact-btn { width: 100%; }
            .featured-product-btn { font-size: 12px; padding: 14px 10px; }
            .faq-card { padding: 16px; }
            .faq-question { font-size: 15px; }
            .faq-answer { font-size: 13px; }
        }
    </style>
<base target="_blank">
</head>
<body>

    <header>
        <a href="/" class="back-btn">&#8592; Back to Shop</a>
        <h1 id="header-title" class="header-title"></h1>
    </header>

    <main>
        <article>
            <div class="card product-image-container">
                <div class="image-wrapper">
                    <span class="image-badge" id="img-badge">In Stock</span>
                    <img id="p-img" src="" alt="" class="product-image">
                </div>
            </div>

            <section class="card product-details">
                <h2 id="p-name" class="product-title"></h2>
                <p id="p-subtitle" class="product-subtitle"></p>

                <div class="section-label">Key Features</div>
                <div id="feature-bubbles"></div>

                <p id="p-desc" class="product-description"></p>

                <div class="section-label">Product Details and FAQ</div>
                <div id="faq-container" class="faq-section"></div>

                <div class="section-label">Choose Your Package</div>
                <div id="pricing-tiers"></div>

                <div class="buy-buttons">
                    <a id="btn-wa" href="#" class="contact-btn btn-whatsapp" target="_blank" rel="noopener">
                        <span style="font-size:20px;">&#128172;</span> WhatsApp
                    </a>
                    <a id="btn-tg" href="#" class="contact-btn btn-telegram" target="_blank" rel="noopener">
                        <span style="font-size:20px;">&#9992;</span> Telegram
                    </a>
                </div>

                <div class="section-label" style="margin-top:32px;">You May Also Like</div>
                <div id="featured-products" class="featured-products-grid"></div>

                <div id="footer-links"></div>
            </section>
        </article>
    </main>

    <div class="disclaimer-bar">
        <p id="disclaimer-text"></p>
    </div>

    <script>
        const PRODUCT_CONFIG = {
            name: "Premium Research Chemical",
            subtitle: "Lab-Grade Purity | Discreet Worldwide Delivery",
            imageSrc: "/images/product.jpg",
            imageAlt: "Premium research chemical product",
            imageBadge: "In Stock",
            seoTitle: "Buy Premium Research Chemical | 247 AVL Plug",
            seoDescription: "Premium research chemical with lab-grade purity. Discreet packaging, worldwide delivery. Order now at 247 AVL Plug!",
            urlSlug: "premium-research-chemical.html",
            pricingTiers: [
                { label: "1 Gram",    price: "$49.99",              badge: "" },
                { label: "5 Grams",   price: "$199.99",            badge: "Save 20%" },
                { label: "10 Grams",  price: "$349.99",            badge: "Best Value" },
                { label: "25 Grams",  price: "$749.99",            badge: "Bulk" },
                { label: "50+ Grams", price: "Contact for Quote",  badge: "Wholesale" }
            ],
            defaultTierIndex: 0,
            description: "Premium research chemical sourced for maximum purity and consistency. Every batch is lab-tested, vacuum-sealed in discreet packaging, and shipped to any address worldwide with full tracking.",
            faqs: [
                { question: "How discreet is the packaging?", answer: "All orders are shipped in plain, unmarked packaging with no indication of contents. We use vacuum-sealed bags inside standard boxes to ensure complete privacy and odor-proof delivery." },
                { question: "How long does delivery take?", answer: "Domestic orders typically arrive within 3 to 5 business days. International shipping ranges from 7 to 14 business days depending on your location. Tracking is provided for every shipment." },
                { question: "What payment methods do you accept?", answer: "We accept Bitcoin, Ethereum, and other major cryptocurrencies for maximum privacy. Contact us via WhatsApp or Telegram for payment instructions after selecting your package." },
                { question: "Is this product legal in my country?", answer: "It is your responsibility to verify the legal status of research chemicals in your jurisdiction before ordering. We sell strictly for research purposes and comply with all applicable laws." }
            ],
            features: ["High Purity", "Discreet Packaging", "Any Address Delivery"],
            featureEmojis: ["&#128142;", "&#128230;", "&#128666;"],
            whatsappNumber: "13254365583",
            telegramHandle: "datplugg",
            featuredProducts: [
                { name: "Research Chem A", url: "/products/chem-a.html" },
                { name: "Research Chem B", url: "/products/chem-b.html" },
                { name: "Research Chem C", url: "/products/chem-c.html" },
                { name: "Research Chem D", url: "/products/chem-d.html" }
            ],
            footerLinks: [
                { text: "Privacy Policy", url: "/privacy-policy.html" },
                { text: "Payment and Refund Policy", url: "/payment-refund-policy.html" },
                { text: "Terms and Conditions", url: "/terms-and-conditions.html" },
                { text: "Shipping and Delivery Policies", url: "/shipping-delivery-policy.html" }
            ],
            disclaimer: "All products sold on this website are intended for research purposes only. By purchasing, you confirm that you are at least 18 years of age and will use these products strictly in accordance with all applicable local, state, and federal laws. We do not condone the misuse of any research chemicals. The vendor assumes no liability for the misuse of products.",
            themeColor: "#25D366",
            backgroundColor: "#FF9500"
        };

        document.addEventListener('DOMContentLoaded', function() {
            var p = PRODUCT_CONFIG;
            var baseUrl = "https://247avlplug.com";
            var productUrl = baseUrl + "/products/" + p.urlSlug;
            var imageUrl = p.imageSrc.startsWith('http') ? p.imageSrc : baseUrl + p.imageSrc;

            document.body.style.backgroundColor = p.backgroundColor;
            document.title = p.seoTitle;
            document.getElementById('seo-desc').content = p.seoDescription;
            document.getElementById('seo-canonical').href = productUrl;
            document.getElementById('og-title').content = p.seoTitle;
            document.getElementById('og-desc').content = p.seoDescription;
            document.getElementById('og-image').content = imageUrl;
            document.getElementById('og-url').content = productUrl;
            document.getElementById('header-title').textContent = p.name;
            document.getElementById('p-name').textContent = p.name;
            document.getElementById('p-subtitle').textContent = p.subtitle;
            document.getElementById('p-desc').textContent = p.description;

            var imgEl = document.getElementById('p-img');
            imgEl.src = imageUrl;
            imgEl.alt = p.imageAlt;

            var badgeEl = document.getElementById('img-badge');
            if (p.imageBadge && p.imageBadge.trim() !== "") {
                badgeEl.textContent = p.imageBadge;
                badgeEl.style.display = "inline-block";
            } else {
                badgeEl.style.display = "none";
            }

            document.getElementById('disclaimer-text').innerHTML = '<strong>Disclaimer</strong>' + p.disclaimer;

            /* Feature Cards */
            var featuresContainer = document.getElementById('feature-bubbles');
            featuresContainer.innerHTML = '';
            featuresContainer.style.display = 'flex';
            featuresContainer.style.flexWrap = 'wrap';
            featuresContainer.style.gap = '12px';
            featuresContainer.style.marginBottom = '28px';

            var emojis = p.featureEmojis || ["&#10003;", "&#10003;", "&#10003;"];
            for (var i = 0; i < p.features.length; i++) {
                var card = document.createElement('div');
                card.style.backgroundColor = '#ffffff';
                card.style.border = '1.5px solid #e5e5e7';
                card.style.borderTop = '4px solid #25D366';
                card.style.borderRadius = '14px';
                card.style.padding = '18px 16px';
                card.style.flex = '1 1 140px';
                card.style.minWidth = '140px';
                card.style.textAlign = 'center';
                card.style.boxShadow = '0 2px 8px rgba(0,0,0,0.06)';
                card.innerHTML = '<div style="font-size:28px;margin-bottom:8px;">' + emojis[i] + '</div><div style="font-size:13px;font-weight:700;color:#1d1d1f;line-height:1.4;">' + p.features[i] + '</div>';
                featuresContainer.appendChild(card);
            }

            /* Product Details & FAQ — ORGANIZED CARDS */
            var faqContainer = document.getElementById('faq-container');
            faqContainer.innerHTML = '';
            for (var q = 0; q < p.faqs.length; q++) {
                var card = document.createElement('div');
                card.className = 'faq-card';
                card.innerHTML = '<div class="faq-question">' + p.faqs[q].question + '</div><div class="faq-answer">' + p.faqs[q].answer + '</div>';
                faqContainer.appendChild(card);
            }

            /* Pricing Tiers */
            var tiersContainer = document.getElementById('pricing-tiers');
            tiersContainer.innerHTML = '';
            tiersContainer.style.display = 'flex';
            tiersContainer.style.flexDirection = 'column';
            tiersContainer.style.gap = '10px';
            tiersContainer.style.marginBottom = '28px';

            var currentTier = p.pricingTiers[p.defaultTierIndex];

            for (var i = 0; i < p.pricingTiers.length; i++) {
                (function(tier, index) {
                    var row = document.createElement('div');
                    var isSelected = (index === p.defaultTierIndex);

                    row.style.backgroundColor = isSelected ? '#e8f8ee' : '#ffffff';
                    row.style.border = isSelected ? '2px solid #25D366' : '2px solid #d1d1d6';
                    row.style.borderLeft = isSelected ? '5px solid #25D366' : '2px solid #d1d1d6';
                    row.style.borderRadius = '12px';
                    row.style.padding = '16px 18px';
                    row.style.cursor = 'pointer';
                    row.style.display = 'flex';
                    row.style.alignItems = 'center';
                    row.style.justifyContent = 'space-between';
                    row.style.gap = '12px';
                    row.style.boxShadow = isSelected ? '0 4px 12px rgba(37,211,102,0.15)' : '0 1px 4px rgba(0,0,0,0.04)';
                    row.style.transition = 'all 0.2s ease';

                    var radioColor = isSelected ? '#25D366' : '#d1d1d6';
                    var radioInner = isSelected ? '<div style="width:10px;height:10px;background:#fff;border-radius:50%;"></div>' : '';
                    var badgeHtml = '';
                    if (tier.badge && tier.badge.trim() !== '') {
                        badgeHtml = '<span style="font-size:11px;background:#25D366;color:#fff;padding:3px 10px;border-radius:50px;font-weight:800;text-transform:uppercase;letter-spacing:0.5px;white-space:nowrap;">' + tier.badge + '</span>';
                    }

                    row.innerHTML = '<div style="display:flex;align-items:center;gap:12px;flex:1;"><div style="width:22px;height:22px;border-radius:50%;border:3px solid ' + radioColor + ';background:' + radioColor + ';display:flex;align-items:center;justify-content:center;flex-shrink:0;">' + radioInner + '</div><div style="display:flex;align-items:center;gap:8px;flex-wrap:wrap;"><span style="font-weight:700;font-size:15px;color:#1d1d1f;white-space:nowrap;">' + tier.label + '</span>' + badgeHtml + '</div></div><span style="font-weight:800;font-size:17px;color:#1faa52;white-space:nowrap;flex-shrink:0;">' + tier.price + '</span>';

                    row.addEventListener('click', function() {
                        var allRows = tiersContainer.children;
                        for (var j = 0; j < allRows.length; j++) {
                            var r = allRows[j];
                            r.style.backgroundColor = '#ffffff';
                            r.style.border = '2px solid #d1d1d6';
                            r.style.borderLeft = '2px solid #d1d1d6';
                            r.style.boxShadow = '0 1px 4px rgba(0,0,0,0.04)';
                            var radio = r.querySelector('div > div');
                            if (radio) { radio.style.borderColor = '#d1d1d6'; radio.style.background = '#d1d1d6'; radio.innerHTML = ''; }
                        }
                        row.style.backgroundColor = '#e8f8ee';
                        row.style.border = '2px solid #25D366';
                        row.style.borderLeft = '5px solid #25D366';
                        row.style.boxShadow = '0 4px 12px rgba(37,211,102,0.15)';
                        var activeRadio = row.querySelector('div > div');
                        if (activeRadio) { activeRadio.style.borderColor = '#25D366'; activeRadio.style.background = '#25D366'; activeRadio.innerHTML = '<div style="width:10px;height:10px;background:#fff;border-radius:50%;"></div>'; }
                        updateLinks(tier);
                    });

                    tiersContainer.appendChild(row);
                })(p.pricingTiers[i], i);
            }

            /* Featured Products */
            var featuredContainer = document.getElementById('featured-products');
            featuredContainer.innerHTML = '';

            for (var f = 0; f < p.featuredProducts.length; f++) {
                var fp = p.featuredProducts[f];

                var link = document.createElement('a');
                link.href = fp.url;
                link.className = 'featured-product-btn';
                link.textContent = fp.name;

                featuredContainer.appendChild(link);
            }

            /* Footer Policy Links */
            var footerContainer = document.getElementById('footer-links');
            footerContainer.innerHTML = '';
            footerContainer.style.marginTop = '28px';
            footerContainer.style.paddingTop = '22px';
            footerContainer.style.borderTop = '1px solid #e5e5e7';
            footerContainer.style.display = 'flex';
            footerContainer.style.flexWrap = 'wrap';
            footerContainer.style.justifyContent = 'center';
            footerContainer.style.gap = '8px 20px';

            for (var k = 0; k < p.footerLinks.length; k++) {
                var link = document.createElement('a');
                link.href = p.footerLinks[k].url;
                link.textContent = p.footerLinks[k].text;
                link.style.color = '#86868b';
                link.style.fontSize = '12px';
                link.style.fontWeight = '600';
                link.style.textDecoration = 'none';
                link.style.whiteSpace = 'nowrap';
                link.addEventListener('mouseenter', function() { this.style.color = '#25D366'; });
                link.addEventListener('mouseleave', function() { this.style.color = '#86868b'; });
                footerContainer.appendChild(link);
            }

            function updateLinks(selectedTier) {
                var waMessage = encodeURIComponent('Hi! I want to buy the ' + p.name + ' (' + selectedTier.label + ' - ' + selectedTier.price + ') from 247avlplug.com');
                document.getElementById('btn-wa').href = 'https://wa.me/' + p.whatsappNumber + '?text=' + waMessage;
                var tgMessage = encodeURIComponent('Hi! I want to buy the ' + p.name + ' (' + selectedTier.label + ' - ' + selectedTier.price + ') from 247avlplug.com');
                document.getElementById('btn-tg').href = 'https://t.me/' + p.telegramHandle + '?text=' + tgMessage;
            }

            updateLinks(currentTier);
        });
    </script>
</body>
</html>
